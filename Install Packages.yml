- name: Install Packages
  hosts: all
  gather_facts: no
  tasks:
    - name: Install httpd service
      package:
        name: httpd
        state: installed
    
    - name: enable httpd service
      package:
        name: httpd
        state: started
